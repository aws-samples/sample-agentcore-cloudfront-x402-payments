# Agent API Configuration
# Copy this file to .env.local and fill in your values

# Backend API endpoint (the payer-agent API server)
# Start the backend with: cd payer-agent && python -m agent.api_server
VITE_API_ENDPOINT=http://localhost:8080

# AWS Region (for display purposes only - backend handles AWS auth)
VITE_AWS_REGION=us-west-2
