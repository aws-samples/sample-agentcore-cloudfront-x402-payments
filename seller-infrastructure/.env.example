# AWS Configuration
AWS_ACCOUNT_ID=your_aws_account_id
AWS_REGION=us-east-1

# CDK Configuration
CDK_DEFAULT_ACCOUNT=your_aws_account_id
CDK_DEFAULT_REGION=us-east-1

# Payment Configuration
# Wallet address on Base Sepolia to receive payments
# CDK injects this into the Lambda@Edge bundle at deploy time
# If not set, falls back to the default address in content-config.ts
PAYMENT_RECIPIENT_ADDRESS=your_wallet_address
