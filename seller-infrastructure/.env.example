# AWS Configuration
AWS_ACCOUNT_ID=your_aws_account_id
AWS_REGION=us-east-1

# CDK Configuration
CDK_DEFAULT_ACCOUNT=your_aws_account_id
CDK_DEFAULT_REGION=us-east-1

# Payment Configuration
# Your wallet address on Base Sepolia to receive payments
# Create one at https://portal.cdp.coinbase.com/ or use MetaMask
# Set this in lib/lambda-edge/content-config.ts → DEFAULT_PAY_TO
# (Lambda@Edge cannot use environment variables at runtime)
PAYMENT_RECIPIENT_ADDRESS=your_wallet_address
